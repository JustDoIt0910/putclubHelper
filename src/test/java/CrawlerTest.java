import PutclubHelper.crawler.Crawler;
import org.junit.Test;

public class CrawlerTest {
    @Test
    public void test() {
        Crawler crawler = new Crawler(null);
        crawler.Craw();
    }
}
